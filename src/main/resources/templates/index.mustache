{{>layout/header}}

<h1>Stuffy Address Book </h1>
<h3>(고리타분한 주소록)</h3>
<div class="col-md-12">
    <div class="row">
        <div class="col-md-6">
            <a href="/posts/templates/save" role="button" class="btn btn-primary">글 등록</a>
<!--            {{#userName}}-->
<!--                Logged in as : <span id="user">{{userName}}</span>-->
<!--                <a href="/logout" class="btn btn-info active" role="button">Logout</a>-->
<!--            {{/userName}}-->
<!--            {{^userName}}-->
<!--                <a href ="/oauth2/authorization/google" class="btn btn-success active"-->
<!--                   role="button"> Google Login</a>-->
<!--            {{/userName}}-->
        </div>
    </div>
    <br>
    <!-- 목록 출력 영역 -->
    <table class="table table-horizontal table-bordered">
        <thead class="thead-strong">
        <tr>
            <th>id</th>
            <th>이름</th>
            <th>나이</th>
            <th>연락처</th>
            <th>최종수정일</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#posts}}
            <tr>
                <td>{{id}}</td>
                <td><a href="/posts/update/{{id}}">{{name}}</a></td>
                <td>{{age}}</td>
                <td>{{phone_number}}</td>
                <td>{{modifiedDate}}</td>
            </tr>
        {{/posts}}
        </tbody>
    </table>
</div>



{{>layout/footer}}